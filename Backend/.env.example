# =============================================================================
# ERP BACKEND ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# SERVER CONFIGURATION
NODE_ENV=development
PORT=3000
API_PREFIX=/api/v1

# SUPABASE DATABASE (Required)
# Get from: https://supabase.com → Your Project → Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# JWT AUTHENTICATION
# Generate: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-change-this
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# CORS
CORS_ORIGINS=http://localhost:3001,http://localhost:3000

# =============================================================================
# LOGISTICS PROVIDERS
# =============================================================================

# GAAU BESI COURIER API
# Production: https://delivery.gaaubesi.com/api/v1
# Testing: https://testing.gaaubesi.com.np/api/v1
GAAU_BESI_API_URL=https://testing.gaaubesi.com.np/api/v1
GAAU_BESI_API_TOKEN=your-gaau-besi-api-token

# NCM (NEPAL CAN MOVE) COURIER API
# Production Portal: https://portal.nepalcanmove.com
# Get token from: portal.nepalcanmove.com/accounts/vendor/api
NCM_API_URL=https://portal.nepalcanmove.com/api/v2
NCM_API_TOKEN=your-ncm-api-token
NCM_SOURCE_BRANCH=TINKUNE
NCM_VENDOR_ID=34465

# Company details for logistics
COMPANY_PHONE=9802359033

# =============================================================================
# WEBHOOKS (P0 SECURITY - HMAC Verification)
# =============================================================================

# SHOPIFY WEBHOOK SECRET
# Get from: Shopify Admin → Settings → Notifications → Webhooks → Signing Secret
SHOPIFY_WEBHOOK_SECRET=your-shopify-webhook-secret

# WOOCOMMERCE WEBHOOK SECRET  
# Set when creating webhook in WooCommerce → Settings → Advanced → Webhooks
WOOCOMMERCE_WEBHOOK_SECRET=your-woocommerce-webhook-secret

# GENERIC WEBHOOK SECRET (for custom integrations)
WEBHOOK_SECRET=your-generic-webhook-secret
